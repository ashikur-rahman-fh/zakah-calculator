import React from "react";

import ProtectedAssetClient from "./AssetClient";

const AssetPage = () => {
  return (
    <React.Fragment>
      <ProtectedAssetClient />
    </React.Fragment>
  );
};

export default AssetPage;
